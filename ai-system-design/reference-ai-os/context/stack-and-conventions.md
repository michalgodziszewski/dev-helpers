# Stack and Conventions

Durable, cross-project technical conventions — tooling, coding standards, recurring patterns —
that aren't already captured in `.claude/GUIDELINES.md` or a project's own
`context/projects/<name>/project-overview.md`. Filled in over time as real conventions
accumulate; not pre-filled with guesses.
